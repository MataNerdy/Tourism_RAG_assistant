# Notebooks

The notebooks are preserved as exploratory project history.

Reusable implementation has been moved into `src/tourism_rag_assistant/`. Thin compatibility wrappers remain in `src/*.py` so older notebook imports such as `from config import CONFIG` and `from rag_pipeline import answer_with_tourism_rag` continue to work after installing the project or adding `src/` to `PYTHONPATH`.

